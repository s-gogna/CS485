Programming Assignment Four

Camera Calibration:
	- All images should be in a folder named "data" and all filenames should be unchanged
	- $ g++ main.cpp `pkg-config --cflags --libs opencv`
	- The results will be displayed on the terminal screen.
	- In the source file, there is a directive that will toggle between printing out data 
	points to generate the CSV file and summary results.
